apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "argos-service.fullname" . }}-test-connection"
  labels:
    {{- include "argos-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "argos-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
