apiVersion: v1
kind: Service
metadata:
  name: {{ include "oauth-stub.fullname" . }}
  labels:
    {{- include "oauth-stub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oauth-stub.selectorLabels" . | nindent 4 }}
