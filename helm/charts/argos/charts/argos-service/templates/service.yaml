apiVersion: v1
kind: Service
metadata:
  name: {{ include "argos-service.fullname" . }}
  labels:
    {{- include "argos-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argos-service.selectorLabels" . | nindent 4 }}
