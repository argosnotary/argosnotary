apiVersion: v1
kind: Service
metadata:
  name: {{ include "argos-frontend.fullname" . }}
  labels:
    {{- include "argos-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argos-frontend.selectorLabels" . | nindent 4 }}
